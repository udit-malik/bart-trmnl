{% assign origin_name = "12th St. Oakland" %}
{% assign destination_name = "16th St. Mission" %}

<div class="layout layout--col gap--small">
  {% if root.schedule.request.trip %}
    {% assign trips = root.schedule.request.trip %}
    <div class="grid grid--cols-3 gap--small">

      <!-- LEFT: Summary + Main Trip (spans 2 cols) -->
      <div class="layout layout--col gap--small col--span-2 m--2">

        <!-- Summary Card -->
        <div class="item bg--white rounded--base mt--4 p--2" style="border: 2px solid #000; flex: 1;">
          <div class="content layout layout--col gap--small">
            <div class="title title--large clamp--1">{{ origin_name }} → {{ destination_name }}</div>
            {% assign seen = "" %}
            {% for trip in trips limit:4 %}
              {% assign heading_lower = trip.leg['@trainHeadStation'] | downcase %}
              {% if heading_lower contains 'millbrae' %}{% assign abbr = 'MLBR' %}
              {% elsif heading_lower contains 'daly' %}{% assign abbr = 'DALY' %}
              {% elsif heading_lower contains 'sfo' or heading_lower contains 'francisco inter' %}{% assign abbr = 'SFIA' %}
              {% elsif heading_lower contains 'richmond' %}{% assign abbr = 'RICH' %}
              {% else %}{% assign abbr = trip.leg['@trainHeadStation'] %}{% endif %}
              {% unless seen contains abbr %}
                {% assign seen = seen | append: abbr | append: "|" %}
                <div class="flex flex--middle gap--small">
                  <span class="label label--outline">{{ abbr }}</span>
                  <span class="label">{{ trip['@tripTime'] }} min</span>
                </div>
              {% endunless %}
            {% endfor %}
          </div>
        </div>

        <!-- Main Trip Card -->
        {% assign main_trip = trips[0] %}
        {% assign heading_lower = main_trip.leg['@trainHeadStation'] | downcase %}
        {% if heading_lower contains 'millbrae' %}{% assign abbr = 'MLBR' %}
        {% elsif heading_lower contains 'daly' %}{% assign abbr = 'DALY' %}
        {% elsif heading_lower contains 'sfo' or heading_lower contains 'francisco inter' %}{% assign abbr = 'SFIA' %}
        {% elsif heading_lower contains 'richmond' %}{% assign abbr = 'RICH' %}
        {% else %}{% assign abbr = main_trip.leg['@trainHeadStation'] %}{% endif %}
        <div class="item bg--black rounded--medium p--2" style="flex: 2;">
          <div class="content layout layout--col gap--small">
            <span class="label bg--white rounded--small p--1" style="align-self: flex-start;">{{ abbr }}</span>
            <div class="value value--xxlarge text--white">{{ main_trip['@origTimeMin'] }}</div>
            <div class="label text--gray-60">DEPART</div>
            <div style="border-top: 1px dashed rgba(255,255,255,0.3); width: 100%; margin: 2px 0;"></div>
            <div class="label text--gray-60">ARRIVE {{ main_trip['@destTimeMin'] }}</div>
          </div>
        </div>
      </div>

      <!-- RIGHT: Next 3 trips -->
      <div class="layout layout--col gap--small pt--6 stretch-y">
        {% for trip in trips offset:1 limit:3 %}
          {% assign heading_lower = trip.leg['@trainHeadStation'] | downcase %}
          {% if heading_lower contains 'millbrae' %}{% assign abbr = 'MLBR' %}
          {% elsif heading_lower contains 'daly' %}{% assign abbr = 'DALY' %}
          {% elsif heading_lower contains 'sfo' or heading_lower contains 'francisco inter' %}{% assign abbr = 'SFIA' %}
          {% elsif heading_lower contains 'richmond' %}{% assign abbr = 'RICH' %}
          {% else %}{% assign abbr = trip.leg['@trainHeadStation'] %}{% endif %}
          <div class="item bg--black rounded--medium p--2" style="flex: 1;">
            <div class="content layout layout--col gap--small">
              <span class="label bg--white rounded--small p--1" style="align-self: flex-start;">{{ abbr }}</span>
              <div class="value value--large text--white">{{ trip['@origTimeMin'] }}</div>
              <div style="border-top: 1px dashed rgba(255,255,255,0.3); width: 100%; margin: 2px 0;"></div>
              <div class="label text--gray-60">ARRIVE {{ trip['@destTimeMin'] }}</div>
            </div>
          </div>
        {% endfor %}
      </div>

    </div>
  {% else %}
    <div class="item">
      <div class="content layout layout--col gap--small">
        <div class="label">No departures available</div>
        {% if root.message.error %}<div class="label label--small">{{ root.message.error.text }}</div>{% endif %}
      </div>
    </div>
  {% endif %}

  <div class="footer">
    <div class="divider"></div>
    <div class="label label--small">Real-time BART schedule</div>
  </div>
</div>
